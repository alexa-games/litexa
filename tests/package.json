{
  "name": "@litexa/integration-tests",
  "version": "0.9.0",
  "description": "Integration tests for the packages.",
  "scripts": {
    "build": "npm run clean && npm install",
    "clean": "npx --ignore-existing rimraf node_modules",
    "coverage": "npx nyc npm test && node ../cacheCoverage.js",
    "test": "npx mocha './features/*.spec.coffee'",
    "test:file": "npx mocha"
  },
  "author": "Amazon",
  "license": "Apache-2.0",
  "private": true
}
