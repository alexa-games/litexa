{
  "name": "{name}",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean": "npx --ignore-existing rimraf ./node_modules && npm install",
    "deploy": "npm run clean && litexa deploy"
  },
  "dependencies": {
    "{name}-lib": "file:../lib"
  },
  "author": "Amazon",
  "license": "ISC"
}
